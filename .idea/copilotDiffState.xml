<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/force-app/main/default/aura/createDiscountForm/createDiscountFormCancelEvent.evt">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/force-app/main/default/aura/createDiscountForm/createDiscountFormCancelEvent.evt" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/CronValidator.cls">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/CronValidator.cls" />
              <option name="originalContent" value="public class CronValidator {&#10;&#10;    private static final Map&lt;String, Integer&gt; DAY_OF_WEEK_MAP = new Map&lt;String, Integer&gt;{&#10;            'SUN' =&gt; 1, 'MON' =&gt; 2, 'TUE' =&gt; 3, 'WED' =&gt; 4, 'THU' =&gt; 5, 'FRI' =&gt; 6, 'SAT' =&gt; 7&#10;    };&#10;&#10;    public static Boolean isDue(String cronExpression, TimeZone userTimeZone) {&#10;        Datetime now = Datetime.now();&#10;&#10;        List&lt;String&gt; parts = cronExpression.split(' ');&#10;        if (parts.size() &lt; 6) {&#10;            System.debug(LoggingLevel.ERROR, 'Nieprawidłowy format cron. Oczekiwano co najmniej 6 części.');&#10;            return false;&#10;        }&#10;&#10;        Integer currentSecond = Integer.valueOf(now.format('s', userTimeZone.getID()));&#10;        Integer currentMinute = Integer.valueOf(now.format('m', userTimeZone.getID()));&#10;        Integer currentHour = Integer.valueOf(now.format('H', userTimeZone.getID()));&#10;        Integer currentDayOfMonth = Integer.valueOf(now.format('d', userTimeZone.getID()));&#10;        Integer currentMonth = Integer.valueOf(now.format('M', userTimeZone.getID()));&#10;        Integer currentDayOfWeek = Integer.valueOf(now.format('u', userTimeZone.getID())) == 7 ? 1 : Integer.valueOf(now.format('u', userTimeZone.getID())) + 1;&#10;        Integer currentYear = Integer.valueOf(now.format('yyyy', userTimeZone.getID()));&#10;&#10;        Boolean dayOfMonthIsWildcard = (parts[3] == '?');&#10;        Boolean dayOfWeekIsWildcard = (parts[5] == '?');&#10;&#10;        if (dayOfMonthIsWildcard == dayOfWeekIsWildcard) {&#10;            return false;&#10;        }&#10;&#10;        Boolean secondsMatch = checkValue(currentSecond, parts[0]);&#10;        Boolean minutesMatch = checkValue(currentMinute, parts[1]);&#10;        Boolean hoursMatch = checkValue(currentHour, parts[2]);&#10;        Boolean monthsMatch = checkValue(currentMonth, parts[4]);&#10;        Boolean yearsMatch = (parts.size() &lt; 7 || checkValue(currentYear, parts[6]));&#10;&#10;        Boolean dayOfMonthMatch = dayOfMonthIsWildcard ? true : checkValue(currentDayOfMonth, parts[3]);&#10;        Boolean dayOfWeekMatch = dayOfWeekIsWildcard ? true : checkDayOfWeek(currentDayOfWeek, parts[5]);&#10;&#10;        if (dayOfMonthIsWildcard) {&#10;            return secondsMatch &amp;&amp; minutesMatch &amp;&amp; hoursMatch &amp;&amp; monthsMatch &amp;&amp; dayOfWeekMatch &amp;&amp; yearsMatch;&#10;        } else {&#10;            return secondsMatch &amp;&amp; minutesMatch &amp;&amp; hoursMatch &amp;&amp; monthsMatch &amp;&amp; dayOfMonthMatch &amp;&amp; yearsMatch;&#10;        }&#10;    }&#10;    private static Boolean checkValue(Integer value, String cronPart) {&#10;        if (cronPart == '*' || cronPart == '?') {&#10;            return true;&#10;        }&#10;&#10;        if (cronPart.contains(',')) {&#10;            for (String part : cronPart.split(',')) {&#10;                if (checkValue(value, part)) {&#10;                    return true;&#10;                }&#10;            }&#10;            return false;&#10;        }&#10;&#10;        if (cronPart.contains('/')) {&#10;            List&lt;String&gt; stepParts = cronPart.split('/');&#10;            Integer step = Integer.valueOf(stepParts[1]);&#10;            if (stepParts[0] == '*') {&#10;                return (Math.mod(value, step) == 0);&#10;            } else {&#10;                if (stepParts[0].contains('-')) {&#10;                    List&lt;String&gt; rangeParts = stepParts[0].split('-');&#10;                    Integer start = Integer.valueOf(rangeParts[0]);&#10;                    if (value &gt;= start) {&#10;                        return (Math.mod(value - start, step) == 0);&#10;                    }&#10;                }&#10;            }&#10;            return false;&#10;        }&#10;&#10;        if (cronPart.contains('-')) {&#10;            List&lt;String&gt; rangeParts = cronPart.split('-');&#10;            Integer start = Integer.valueOf(rangeParts[0]);&#10;            Integer end = Integer.valueOf(rangeParts[1]);&#10;            return (value &gt;= start &amp;&amp; value &lt;= end);&#10;        }&#10;&#10;        return Integer.valueOf(cronPart) == value;&#10;    }&#10;&#10;    private static Boolean checkDayOfWeek(Integer currentDayNum, String cronPart) {&#10;        if (cronPart == '*' || cronPart == '?') {&#10;            return true;&#10;        }&#10;&#10;        cronPart = cronPart.toUpperCase();&#10;&#10;        if (cronPart.contains(',')) {&#10;            for (String part : cronPart.split(',')) {&#10;                if (checkDayOfWeek(currentDayNum, part)) {&#10;                    return true;&#10;                }&#10;            }&#10;            return false;&#10;        }&#10;&#10;        if (cronPart.contains('-')) {&#10;            List&lt;String&gt; rangeParts = cronPart.split('-');&#10;            Integer start = DAY_OF_WEEK_MAP.get(rangeParts[0]);&#10;            Integer end = DAY_OF_WEEK_MAP.get(rangeParts[1]);&#10;&#10;            if (start == null || end == null) return false;&#10;&#10;            if (start &lt;= end) {&#10;                return currentDayNum &gt;= start &amp;&amp; currentDayNum &lt;= end;&#10;            } else {&#10;                return currentDayNum &gt;= start || currentDayNum &lt;= end;&#10;            }&#10;        }&#10;&#10;        return DAY_OF_WEEK_MAP.get(cronPart) == currentDayNum;&#10;    }&#10;}" />
              <option name="updatedContent" value="public class CronValidator {&#10;&#10;    private static final Map&lt;String, Integer&gt; DAY_OF_WEEK_MAP = new Map&lt;String, Integer&gt;{&#10;            'SUN' =&gt; 1, 'MON' =&gt; 2, 'TUE' =&gt; 3, 'WED' =&gt; 4, 'THU' =&gt; 5, 'FRI' =&gt; 6, 'SAT' =&gt; 7&#10;    };&#10;&#10;    public static Boolean isDue(String cronExpression, TimeZone userTimeZone) {&#10;        Datetime now = Datetime.now();&#10;&#10;        List&lt;String&gt; parts = cronExpression.split(' ');&#10;        if (parts.size() &lt; 6) {&#10;            System.debug(LoggingLevel.ERROR, 'Nieprawidłowy format cron. Oczekiwano co najmniej 6 części.');&#10;            return false;&#10;        }&#10;&#10;        Integer currentSecond = Integer.valueOf(now.format('s', userTimeZone.getID()));&#10;        Integer currentMinute = Integer.valueOf(now.format('m', userTimeZone.getID()));&#10;        Integer currentHour = Integer.valueOf(now.format('H', userTimeZone.getID()));&#10;        Integer currentDayOfMonth = Integer.valueOf(now.format('d', userTimeZone.getID()));&#10;        Integer currentMonth = Integer.valueOf(now.format('M', userTimeZone.getID()));&#10;        Integer currentDayOfWeek = Integer.valueOf(now.format('u', userTimeZone.getID())) == 7 ? 1 : Integer.valueOf(now.format('u', userTimeZone.getID())) + 1;&#10;        Integer currentYear = Integer.valueOf(now.format('yyyy', userTimeZone.getID()));&#10;&#10;        Boolean dayOfMonthIsWildcard = (parts[3] == '?');&#10;        Boolean dayOfWeekIsWildcard = (parts[5] == '?');&#10;&#10;        if (dayOfMonthIsWildcard == dayOfWeekIsWildcard) {&#10;            return false;&#10;        }&#10;&#10;        Boolean secondsMatch = checkValue(currentSecond, parts[0]);&#10;        Boolean minutesMatch = checkValue(currentMinute, parts[1]);&#10;        Boolean hoursMatch = checkValue(currentHour, parts[2]);&#10;        Boolean monthsMatch = checkValue(currentMonth, parts[4]);&#10;        Boolean yearsMatch = (parts.size() &lt; 7 || checkValue(currentYear, parts[6]));&#10;&#10;        Boolean dayOfMonthMatch = dayOfMonthIsWildcard ? true : checkValue(currentDayOfMonth, parts[3]);&#10;        Boolean dayOfWeekMatch = dayOfWeekIsWildcard ? true : checkDayOfWeek(currentDayOfWeek, parts[5]);&#10;&#10;        if (dayOfMonthIsWildcard) {&#10;            return secondsMatch &amp;&amp; minutesMatch &amp;&amp; hoursMatch &amp;&amp; monthsMatch &amp;&amp; dayOfWeekMatch &amp;&amp; yearsMatch;&#10;        } else {&#10;            return secondsMatch &amp;&amp; minutesMatch &amp;&amp; hoursMatch &amp;&amp; monthsMatch &amp;&amp; dayOfMonthMatch &amp;&amp; yearsMatch;&#10;        }&#10;    }&#10;    private static Boolean checkValue(Integer value, String cronPart) {&#10;        if (cronPart == '*' || cronPart == '?') {&#10;            return true;&#10;        }&#10;&#10;        if (cronPart.contains(',')) {&#10;            for (String part : cronPart.split(',')) {&#10;                if (checkValue(value, part)) {&#10;                    return true;&#10;                }&#10;            }&#10;            return false;&#10;        }&#10;&#10;        if (cronPart.contains('/')) {&#10;            List&lt;String&gt; stepParts = cronPart.split('/');&#10;            Integer step = Integer.valueOf(stepParts[1]);&#10;            if (stepParts[0] == '*') {&#10;                return (Math.mod(value, step) == 0);&#10;            } else {&#10;                if (stepParts[0].contains('-')) {&#10;                    List&lt;String&gt; rangeParts = stepParts[0].split('-');&#10;                    Integer start = Integer.valueOf(rangeParts[0]);&#10;                    if (value &gt;= start) {&#10;                        return (Math.mod(value - start, step) == 0);&#10;                    }&#10;                }&#10;            }&#10;            return false;&#10;        }&#10;&#10;        if (cronPart.contains('-')) {&#10;            List&lt;String&gt; rangeParts = cronPart.split('-');&#10;            Integer start = Integer.valueOf(rangeParts[0]);&#10;            Integer endVal = Integer.valueOf(rangeParts[1]); // changed from 'end' to 'endVal'&#10;            return (value &gt;= start &amp;&amp; value &lt;= endVal);&#10;        }&#10;&#10;        return Integer.valueOf(cronPart) == value;&#10;    }&#10;&#10;    private static Boolean checkDayOfWeek(Integer currentDayNum, String cronPart) {&#10;        if (cronPart == '*' || cronPart == '?') {&#10;            return true;&#10;        }&#10;&#10;        cronPart = cronPart.toUpperCase();&#10;&#10;        if (cronPart.contains(',')) {&#10;            for (String part : cronPart.split(',')) {&#10;                if (checkDayOfWeek(currentDayNum, part)) {&#10;                    return true;&#10;                }&#10;            }&#10;            return false;&#10;        }&#10;&#10;        if (cronPart.contains('-')) {&#10;            List&lt;String&gt; rangeParts = cronPart.split('-');&#10;            Integer start = DAY_OF_WEEK_MAP.get(rangeParts[0]);&#10;            Integer endVal = DAY_OF_WEEK_MAP.get(rangeParts[1]); // changed from 'end' to 'endVal'&#10;&#10;            if (start == null || endVal == null) return false;&#10;&#10;            if (start &lt;= endVal) {&#10;                return currentDayNum &gt;= start &amp;&amp; currentDayNum &lt;= endVal;&#10;            } else {&#10;                return currentDayNum &gt;= start || currentDayNum &lt;= endVal;&#10;            }&#10;        }&#10;&#10;        return DAY_OF_WEEK_MAP.get(cronPart) == currentDayNum;&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/OrderItemTriggerHandler.cls">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/OrderItemTriggerHandler.cls" />
              <option name="originalContent" value="public with sharing class OrderItemTriggerHandler {&#10;&#10;    public void run() {&#10;        List&lt;OrderItem&gt; itemsToProcess = new List&lt;OrderItem&gt;();&#10;&#10;        if (Trigger.isInsert || Trigger.isUpdate || Trigger.isUndelete) {&#10;            itemsToProcess.addAll((List&lt;OrderItem&gt;) Trigger.new);&#10;        } else if (Trigger.isDelete) {&#10;            itemsToProcess.addAll((List&lt;OrderItem&gt;) Trigger.old);&#10;        }&#10;&#10;        if (!itemsToProcess.isEmpty()) {&#10;            DiscountCalculatorHandler.applyDiscounts(itemsToProcess);&#10;        }&#10;    }&#10;}" />
              <option name="updatedContent" value="public with sharing class OrderItemTriggerHandler {&#10;&#10;    public void run() {&#10;        List&lt;OrderItem&gt; itemsToProcess = new List&lt;OrderItem&gt;();&#10;&#10;        if (Trigger.isInsert || Trigger.isUpdate || Trigger.isUndelete) {&#10;            itemsToProcess.addAll((List&lt;OrderItem&gt;) Trigger.new);&#10;        } else if (Trigger.isDelete) {&#10;            itemsToProcess.addAll((List&lt;OrderItem&gt;) Trigger.old);&#10;        }&#10;&#10;        if (!itemsToProcess.isEmpty()) {&#10;            DiscountCalculatorHandler.applyDiscounts(itemsToProcess);&#10;        }&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/tests/DiscountTestDataFactory.cls">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/tests/DiscountTestDataFactory.cls" />
              <option name="originalContent" value="public class DiscountTestDataFactory extends TestDataFactory {&#10;    protected override SObject prepare() {&#10;        return new Discount__c(&#10;                Name = 'Test Discount',&#10;                Is_Active__c = true,&#10;                Discount_Type__c = ConstUtil.DISCOUNT_TYPE_TIME_BOUND,&#10;                Value_Type__c = ConstUtil.DISCOUNT_VALUE_TYPE_PERCENTAGE,&#10;                Discount_Value__c = 10.0,&#10;                Start_Date__c = Date.today(),&#10;                End_Date__c = Date.today().addDays(30)&#10;        );&#10;    }&#10;&#10;    public override SObject create() {&#10;        Discount__c discount = (Discount__c) prepare();&#10;        insert discount;&#10;        return discount;&#10;    }&#10;&#10;    private class ConditionalDiscountFactory extends DiscountTestDataFactory {&#10;        public override SObject create() {&#10;            Discount__c discount = (Discount__c) prepare();&#10;            discount.Discount_Type__c = ConstUtil.DISCOUNT_TYPE_CONDITIONAL;&#10;            discount.Condition_Logic_Field__c = 'Order.Products_Amount';&#10;            discount.Condition_Logic_Operator__c = '&gt;';&#10;            discount.Condition_Logic_Numeric_Value__c = 1;&#10;            insert discount;&#10;            return discount;&#10;        }&#10;    }&#10;&#10;    private class RecurringDiscountFactory extends DiscountTestDataFactory {&#10;        public override SObject create() {&#10;            Discount__c discount = (Discount__c) prepare();&#10;            discount.Discount_Type__c = ConstUtil.DISCOUNT_TYPE_RECURRING;&#10;            discount.Recurring_Date_Start__c = Datetime.now();&#10;            discount.Recurring_Date_End__c = Datetime.now().addHours(2);&#10;            discount.Recurring_Date_Repeat_By__c = ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY;&#10;            insert discount;&#10;            return discount;&#10;        }&#10;    }&#10;&#10;    private class InactiveDiscountFactory extends DiscountTestDataFactory {&#10;        public override SObject create() {&#10;            Discount__c discount = (Discount__c) prepare();&#10;            discount.Is_Active__c = false;&#10;            insert discount;&#10;            return discount;&#10;        }&#10;    }&#10;&#10;    public static DiscountTestDataFactory conditional() {&#10;        return new ConditionalDiscountFactory();&#10;    }&#10;&#10;    public static DiscountTestDataFactory recurring() {&#10;        return new RecurringDiscountFactory();&#10;    }&#10;&#10;    public static DiscountTestDataFactory inactive() {&#10;        return new InactiveDiscountFactory();&#10;    }&#10;}" />
              <option name="updatedContent" value="public virtual class DiscountTestDataFactory extends TestDataFactory {&#10;    protected override SObject prepare() {&#10;        return new Discount__c(&#10;                Name = 'Test Discount',&#10;                Is_Active__c = true,&#10;                Discount_Type__c = ConstUtil.DISCOUNT_TYPE_TIME_BOUND,&#10;                Value_Type__c = ConstUtil.DISCOUNT_VALUE_TYPE_PERCENTAGE,&#10;                Discount_Value__c = 10.0,&#10;                Start_Date__c = Date.today(),&#10;                End_Date__c = Date.today().addDays(30)&#10;        );&#10;    }&#10;&#10;    public virtual override SObject create() {&#10;        Discount__c discount = (Discount__c) prepare();&#10;        insert discount;&#10;        return discount;&#10;    }&#10;&#10;    private class ConditionalDiscountFactory extends DiscountTestDataFactory {&#10;        public override SObject create() {&#10;            Discount__c discount = (Discount__c) prepare();&#10;            discount.Discount_Type__c = ConstUtil.DISCOUNT_TYPE_CONDITIONAL;&#10;            discount.Condition_Logic_Field__c = 'Order.Products_Amount';&#10;            discount.Condition_Logic_Operator__c = '&gt;';&#10;            discount.Condition_Logic_Numeric_Value__c = 1;&#10;            insert discount;&#10;            return discount;&#10;        }&#10;    }&#10;&#10;    private class RecurringDiscountFactory extends DiscountTestDataFactory {&#10;        public override SObject create() {&#10;            Discount__c discount = (Discount__c) prepare();&#10;            discount.Discount_Type__c = ConstUtil.DISCOUNT_TYPE_RECURRING;&#10;            discount.Recurring_Date_Start__c = Datetime.now();&#10;            discount.Recurring_Date_End__c = Datetime.now().addHours(2);&#10;            discount.Recurring_Date_Repeat_By__c = ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY;&#10;            insert discount;&#10;            return discount;&#10;        }&#10;    }&#10;&#10;    private class InactiveDiscountFactory extends DiscountTestDataFactory {&#10;        public override SObject create() {&#10;            Discount__c discount = (Discount__c) prepare();&#10;            discount.Is_Active__c = false;&#10;            insert discount;&#10;            return discount;&#10;        }&#10;    }&#10;&#10;    public static DiscountTestDataFactory conditional() {&#10;        return new ConditionalDiscountFactory();&#10;    }&#10;&#10;    public static DiscountTestDataFactory recurring() {&#10;        return new RecurringDiscountFactory();&#10;    }&#10;&#10;    public static DiscountTestDataFactory inactive() {&#10;        return new InactiveDiscountFactory();&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/tests/RecurringDateValidatorTests.cls">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/tests/RecurringDateValidatorTests.cls" />
              <option name="originalContent" value="@IsTest&#10;private class RecurringDateValidatorTests {&#10;&#10;    @IsTest&#10;    static void testRecurringDateInvalidWithNullStartDate() {&#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(null)&#10;                .withRecurringDateEnd(Datetime.now().addDays(1))&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(false, isValid, 'Recurring date should be invalid with null start date');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateInvalidWithNullEndDate() {&#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(Datetime.now())&#10;                .withRecurringDateEnd(null)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(false, isValid, 'Recurring date should be invalid with null end date');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithDailyTimeRange() {&#10;        // Create discount valid for current time&#10;        Datetime now = Datetime.now();&#10;        Datetime startTime = now.addHours(-1); // 1 hour before now&#10;        Datetime endTime = now.addHours(1);    // 1 hour after now&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(startTime)&#10;                .withRecurringDateEnd(endTime)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(true, isValid, 'Recurring date should be valid for daily time range containing current time');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateInvalidWithDailyTimeRangeOutside() {&#10;        // Create discount not valid for current time&#10;        Datetime now = Datetime.now();&#10;        Datetime startTime = now.addHours(-3); // 3 hours before now&#10;        Datetime endTime = now.addHours(-1);   // 1 hour before now&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(startTime)&#10;                .withRecurringDateEnd(endTime)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(false, isValid, 'Recurring date should be invalid for daily time range not containing current time');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithWeeklyCurrentDay() {&#10;        // Create discount valid for current day of week&#10;        Datetime now = Datetime.now();&#10;        String todayName = now.format('EEEE');&#10;        &#10;        // Set start and end to same day of week as today&#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(now)&#10;                .withRecurringDateEnd(now)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_WEEKLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(true, isValid, 'Recurring date should be valid for weekly when current day matches');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithWeeklyRange() {&#10;        // Create discount valid from Monday to Friday&#10;        Datetime monday = getDateForDayOfWeek('Monday');&#10;        Datetime friday = getDateForDayOfWeek('Friday');&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(monday)&#10;                .withRecurringDateEnd(friday)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_WEEKLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        // This test will pass if today is Monday-Friday&#10;        Datetime now = Datetime.now();&#10;        String todayName = now.format('EEEE');&#10;        Boolean shouldBeValid = (todayName == 'Monday' || todayName == 'Tuesday' || &#10;                                todayName == 'Wednesday' || todayName == 'Thursday' || &#10;                                todayName == 'Friday');&#10;        &#10;        System.assertEquals(shouldBeValid, isValid, &#10;            'Recurring date validity should match whether current day is in Monday-Friday range');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithMonthlyCurrentDay() {&#10;        // Create discount valid for current day of month&#10;        Datetime now = Datetime.now();&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(now)&#10;                .withRecurringDateEnd(now)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_MONTHLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(true, isValid, 'Recurring date should be valid for monthly when current day matches');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithMonthlyRange() {&#10;        // Create discount valid from 1st to 15th of month&#10;        Datetime now = Datetime.now();&#10;        Datetime firstDay = Datetime.newInstance(now.year(), now.month(), 1, 12, 0, 0);&#10;        Datetime fifteenthDay = Datetime.newInstance(now.year(), now.month(), 15, 12, 0, 0);&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(firstDay)&#10;                .withRecurringDateEnd(fifteenthDay)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_MONTHLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        // This test will pass if today is between 1st and 15th&#10;        Boolean shouldBeValid = (now.day() &gt;= 1 &amp;&amp; now.day() &lt;= 15);&#10;        &#10;        System.assertEquals(shouldBeValid, isValid, &#10;            'Recurring date validity should match whether current day is in 1st-15th range');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithYearlyCurrentMonth() {&#10;        // Create discount valid for current month&#10;        Datetime now = Datetime.now();&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(now)&#10;                .withRecurringDateEnd(now)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_YEARLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(true, isValid, 'Recurring date should be valid for yearly when current month matches');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithYearlyRange() {&#10;        // Create discount valid from June to August&#10;        Datetime now = Datetime.now();&#10;        Datetime june = Datetime.newInstance(now.year(), 6, 1, 12, 0, 0);&#10;        Datetime august = Datetime.newInstance(now.year(), 8, 1, 12, 0, 0);&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(june)&#10;                .withRecurringDateEnd(august)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_YEARLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        // This test will pass if current month is June, July, or August&#10;        Boolean shouldBeValid = (now.month() &gt;= 6 &amp;&amp; now.month() &lt;= 8);&#10;        &#10;        System.assertEquals(shouldBeValid, isValid, &#10;            'Recurring date validity should match whether current month is in June-August range');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateInvalidWithUnknownRepeatBy() {&#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(Datetime.now())&#10;                .withRecurringDateEnd(Datetime.now().addDays(1))&#10;                .withRecurringDateRepeatBy('Invalid Option')&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(false, isValid, 'Recurring date should be invalid with unknown repeat by option');&#10;    }&#10;&#10;    // Helper method to get a datetime for a specific day of the week&#10;    private static Datetime getDateForDayOfWeek(String dayName) {&#10;        Datetime now = Datetime.now();&#10;        Map&lt;String, Integer&gt; weekdayMap = new Map&lt;String, Integer&gt;{&#10;            'Monday' =&gt; 1, 'Tuesday' =&gt; 2, 'Wednesday' =&gt; 3, 'Thursday' =&gt; 4,&#10;            'Friday' =&gt; 5, 'Saturday' =&gt; 6, 'Sunday' =&gt; 7&#10;        };&#10;        &#10;        Integer targetDay = weekdayMap.get(dayName);&#10;        String currentDayName = now.format('EEEE');&#10;        Integer currentDay = weekdayMap.get(currentDayName);&#10;        &#10;        // Calculate days to add/subtract to get to target day&#10;        Integer daysToAdd = targetDay - currentDay;&#10;        &#10;        return now.addDays(daysToAdd);&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="@IsTest&#10;private class RecurringDateValidatorTests {&#10;&#10;    @IsTest&#10;    static void testRecurringDateInvalidWithNullStartDate() {&#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(null)&#10;                .withRecurringDateEnd(Datetime.now().addDays(1))&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(false, isValid, 'Recurring date should be invalid with null start date');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateInvalidWithNullEndDate() {&#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(Datetime.now())&#10;                .withRecurringDateEnd(null)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(false, isValid, 'Recurring date should be invalid with null end date');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithDailyTimeRange() {&#10;        // Create discount valid for current time&#10;        Datetime now = Datetime.now();&#10;        Datetime startTime = now.addHours(-1); // 1 hour before now&#10;        Datetime endTime = now.addHours(1);    // 1 hour after now&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(startTime)&#10;                .withRecurringDateEnd(endTime)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(true, isValid, 'Recurring date should be valid for daily time range containing current time');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateInvalidWithDailyTimeRangeOutside() {&#10;        // Create discount not valid for current time&#10;        Datetime now = Datetime.now();&#10;        Datetime startTime = now.addHours(-3); // 3 hours before now&#10;        Datetime endTime = now.addHours(-1);   // 1 hour before now&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(startTime)&#10;                .withRecurringDateEnd(endTime)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_DAILY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(false, isValid, 'Recurring date should be invalid for daily time range not containing current time');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithWeeklyCurrentDay() {&#10;        // Create discount valid for current day of week&#10;        Datetime now = Datetime.now();&#10;        String todayName = now.format('EEEE');&#10;        &#10;        // Set start and end to same day of week as today&#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(now)&#10;                .withRecurringDateEnd(now)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_WEEKLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(true, isValid, 'Recurring date should be valid for weekly when current day matches');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithWeeklyRange() {&#10;        // Create discount valid from Monday to Friday&#10;        Datetime monday = getDateForDayOfWeek('Monday');&#10;        Datetime friday = getDateForDayOfWeek('Friday');&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(monday)&#10;                .withRecurringDateEnd(friday)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_WEEKLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        // This test will pass if today is Monday-Friday&#10;        Datetime now = Datetime.now();&#10;        String currentDayName = now.format('EEEE');&#10;        Boolean shouldBeValid = (currentDayName == 'Monday' || currentDayName == 'Tuesday' || &#10;                                currentDayName == 'Wednesday' || currentDayName == 'Thursday' || &#10;                                currentDayName == 'Friday');&#10;        &#10;        System.assertEquals(shouldBeValid, isValid, &#10;            'Recurring date validity should match whether current day is in Monday-Friday range');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithMonthlyCurrentDay() {&#10;        // Create discount valid for current day of month&#10;        Datetime now = Datetime.now();&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(now)&#10;                .withRecurringDateEnd(now)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_MONTHLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(true, isValid, 'Recurring date should be valid for monthly when current day matches');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithMonthlyRange() {&#10;        // Create discount valid from 1st to 15th of month&#10;        Datetime now = Datetime.now();&#10;        Datetime firstDay = Datetime.newInstance(now.year(), now.month(), 1, 12, 0, 0);&#10;        Datetime fifteenthDay = Datetime.newInstance(now.year(), now.month(), 15, 12, 0, 0);&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(firstDay)&#10;                .withRecurringDateEnd(fifteenthDay)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_MONTHLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        // This test will pass if today is between 1st and 15th&#10;        Boolean shouldBeValid = (now.day() &gt;= 1 &amp;&amp; now.day() &lt;= 15);&#10;        &#10;        System.assertEquals(shouldBeValid, isValid, &#10;            'Recurring date validity should match whether current day is in 1st-15th range');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithYearlyCurrentMonth() {&#10;        // Create discount valid for current month&#10;        Datetime now = Datetime.now();&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(now)&#10;                .withRecurringDateEnd(now)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_YEARLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(true, isValid, 'Recurring date should be valid for yearly when current month matches');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateValidWithYearlyRange() {&#10;        // Create discount valid from June to August&#10;        Datetime now = Datetime.now();&#10;        Datetime june = Datetime.newInstance(now.year(), 6, 1, 12, 0, 0);&#10;        Datetime august = Datetime.newInstance(now.year(), 8, 1, 12, 0, 0);&#10;        &#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(june)&#10;                .withRecurringDateEnd(august)&#10;                .withRecurringDateRepeatBy(ConstUtil.DISCOUNT_RECURRING_DATE_REPEAT_BY_YEARLY)&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        // This test will pass if current month is June, July, or August&#10;        Boolean shouldBeValid = (now.month() &gt;= 6 &amp;&amp; now.month() &lt;= 8);&#10;        &#10;        System.assertEquals(shouldBeValid, isValid, &#10;            'Recurring date validity should match whether current month is in June-August range');&#10;    }&#10;&#10;    @IsTest&#10;    static void testRecurringDateInvalidWithUnknownRepeatBy() {&#10;        Discount__c discount = DiscountBuilder.create()&#10;                .withName('Test Discount')&#10;                .withRecurringDateStart(Datetime.now())&#10;                .withRecurringDateEnd(Datetime.now().addDays(1))&#10;                .withRecurringDateRepeatBy('Invalid Option')&#10;                .build();&#10;&#10;        Test.startTest();&#10;        Boolean isValid = RecurringDateValidator.isRecurringDateValidForToday(discount);&#10;        Test.stopTest();&#10;&#10;        System.assertEquals(false, isValid, 'Recurring date should be invalid with unknown repeat by option');&#10;    }&#10;&#10;    // Helper method to get a datetime for a specific day of the week&#10;    private static Datetime getDateForDayOfWeek(String dayName) {&#10;        Datetime now = Datetime.now();&#10;        Map&lt;String, Integer&gt; weekdayMap = new Map&lt;String, Integer&gt;{&#10;            'Monday' =&gt; 1, 'Tuesday' =&gt; 2, 'Wednesday' =&gt; 3, 'Thursday' =&gt; 4,&#10;            'Friday' =&gt; 5, 'Saturday' =&gt; 6, 'Sunday' =&gt; 7&#10;        };&#10;        &#10;        Integer targetDay = weekdayMap.get(dayName);&#10;        String currentDayName = now.format('EEEE');&#10;        Integer currentDay = weekdayMap.get(currentDayName);&#10;        &#10;        // Calculate days to add/subtract to get to target day&#10;        Integer daysToAdd = targetDay - currentDay;&#10;        &#10;        return now.addDays(daysToAdd);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/tests/RecurringDateValidatorTests.cls-meta.xml">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/force-app/main/default/classes/discountCalculator/tests/RecurringDateValidatorTests.cls-meta.xml" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/force-app/main/default/triggers/AppliedDiscount.trigger">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/force-app/main/default/triggers/AppliedDiscount.trigger" />
              <option name="originalContent" value="trigger AppliedDiscount on Applied_Discount__c (before insert, before update) {&#10;    List&lt;Applied_Discount__c&gt; appliedDiscounts = Trigger.new;&#10;    &#10;}" />
              <option name="updatedContent" value="trigger AppliedDiscount on Applied_Discount__c (before insert, before update) {&#10;    List&lt;Applied_Discount__c&gt; appliedDiscounts = Trigger.new;&#10;    &#10;}&#10;&#10;trigger AppliedDiscount on Applied_Discount__c (after insert, after update) {&#10;    Set&lt;Id&gt; orderIds = new Set&lt;Id&gt;();&#10;    for (Applied_Discount__c ad : Trigger.new) {&#10;        if (ad.Order__c != null) {&#10;            orderIds.add(ad.Order__c);&#10;        }&#10;    }&#10;    if (orderIds.isEmpty()) return;&#10;&#10;    // Get all Applied_Discount__c for affected Orders&#10;    Map&lt;Id, List&lt;Applied_Discount__c&gt;&gt; orderToDiscounts = new Map&lt;Id, List&lt;Applied_Discount__c&gt;&gt;();&#10;    for (Applied_Discount__c ad : [SELECT Id, Order__c, Amount_Discounted__c FROM Applied_Discount__c WHERE Order__c IN :orderIds]) {&#10;        if (!orderToDiscounts.containsKey(ad.Order__c)) {&#10;            orderToDiscounts.put(ad.Order__c, new List&lt;Applied_Discount__c&gt;());&#10;        }&#10;        orderToDiscounts.get(ad.Order__c).add(ad);&#10;    }&#10;&#10;    // Get Orders&#10;    List&lt;Order&gt; ordersToUpdate = new List&lt;Order&gt;();&#10;    for (Order o : [SELECT Id, TotalAmount, Order_Amount_After_Discount__c FROM Order WHERE Id IN :orderIds]) {&#10;        List&lt;Applied_Discount__c&gt; discounts = orderToDiscounts.get(o.Id);&#10;        if (discounts == null || discounts.isEmpty()) continue;&#10;        Decimal maxDiscount = 0;&#10;        for (Applied_Discount__c ad : discounts) {&#10;            if (ad.Amount_Discounted__c != null &amp;&amp; ad.Amount_Discounted__c &gt; maxDiscount) {&#10;                maxDiscount = ad.Amount_Discounted__c;&#10;            }&#10;        }&#10;        o.Order_Amount_After_Discount__c = o.TotalAmount != null ? o.TotalAmount - maxDiscount : null;&#10;        ordersToUpdate.add(o);&#10;    }&#10;    if (!ordersToUpdate.isEmpty()) {&#10;        update ordersToUpdate;&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>